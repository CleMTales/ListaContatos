import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    lista: {
        marginTop: 14
    },
    infoImagePress: {
        width: '100%',
        height: '100%',
        position: 'absolute'
    },
});

export default styles;